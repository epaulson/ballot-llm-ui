Never install python packages without first activating the virtual environment in .venv in the top-level directory. 

Remember when you're running a quick python process, like just to fire up a web server to serve the front-end, without running venv you need to call python3

